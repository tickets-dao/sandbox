#!/bin/sh

. default

find "$DATA" -type f ! -name "backup.tar.gz" -exec md5sum '{}' \; > "$STATE_HASH"
find "$DATA" ! -name "backup.tar.gz" > "$STATE_FILE"
_info "consistent state $(md5sum "$STATE_HASH")"
